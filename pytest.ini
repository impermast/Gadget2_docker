[pytest]
testpaths = tests
addopts = -q
markers =
    math: level 1 — unit tests of loaders math on synthetic fixtures (fast, analytic answers)
    golden: level 2 — answer testing against documented golden values (tests/golden/values.json)